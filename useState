import { useState } from "react";

export default function CounterWeb() {
  const [count, setCount] = useState(0);
  return (
    <button onClick={() => setCount(c => c + 1)}>
      Cliquei {count} vez(es)
    </button>
  );
}
